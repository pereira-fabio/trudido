name: trudido
description: "A feature-rich todo app built with Flutter (renamed to Trudido)"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.5+26 # Release tag for v1.3.5 (build 26)

environment:
  sdk: ^3.8.1

# Platform configuration - Android only
platforms:
  android:

# Build metadata (helpful for reproducible builds and support requests)
build_info:
  commit: TBD
  tag: v1.3.2
  flutter: "3.35.5"
  flutter_path: "/c/dev/flutter/bin/flutter"
  build_command: "flutter build apk --release --build-name=1.3.2 --build-number=23"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^3.2.1

  # Local storage
  shared_preferences: ^2.3.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.4

  # UI and utilities
  intl: ^0.20.2
  uuid: ^4.5.0
  flutter_slidable: ^4.0.1
  table_calendar: ^3.1.2
  # Dynamic Material You colors (Android 12+)
  dynamic_color: ^1.8.1
  # Staggered grid for masonry layout
  flutter_staggered_grid_view: ^0.7.0

  # Icons and fonts
  meta: ^1.16.0
  # Removed google_fonts to reduce APK size - using system default fonts (Roboto on Android)

  # Markdown support
  flutter_markdown_plus: ^1.0.7

  # Rich text editor (WYSIWYG)
  flutter_quill: ^11.5.1

  # Open external URLs (e.g., GitHub link in About screen)
  url_launcher: ^6.1.10

  # Self-hosted sync. Already resolved transitively at this version; promoted
  # to a direct dependency because sync imports it, and because it is the one
  # HTTP client that works unchanged on both Android and web.
  http: ^1.6.0

  # (Removed flutter_local_notifications + related timezone/permission packages in favor of pure native Android implementation)

  # File picker for import/export (upgrade to fix v1 embedding + desktop default plugin warnings)
  file_picker: ^10.3.10

  # Permissions handler
  permission_handler: ^12.0.1

  # Image/video picker with camera support
  image_picker: ^1.1.2

  # Video thumbnail generation (actively maintained)
  fc_native_video_thumbnail: ^0.17.2

  # Video player
  video_player: ^2.9.2

  # Audio recording
  record: ^6.2.0
  path: ^1.9.0

  # Keep screen awake (used during voice recording)
  wakelock_plus: ^1.2.8

  # Audio playback
  audioplayers: ^6.0.0

  # PDF generation and export
  pdf: ^3.11.1
  printing: ^5.13.4

  # Encryption for vault notes
  encrypt: ^5.0.3
  flutter_secure_storage: ^10.0.0

  # Biometric authentication for vault access
  local_auth: ^3.0.0

  # Cryptographic hashing for secure password storage
  crypto: ^3.0.3

  # Calendar integration (for DAVx5/Android Calendar sync)
  # Using device_calendar_plus - modern replacement with built-in ProGuard rules
  device_calendar_plus: ^0.3.3
  share_plus: ^12.0.1
  flex_color_picker: ^3.7.2
  highlight: ^0.7.0
  markdown: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation dev dependencies removed temporarily to avoid version solver conflicts with flutter_test

  # Icon generation
  flutter_launcher_icons: ^0.14.4

# Override dependencies to fix compatibility issues
dependency_overrides:
  # Fix record_linux compatibility issue
  record_linux: ^1.3.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # Enable code generation for localization
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - LICENSE
    # App icons (needed at runtime)
    - assets/imagefiles/trudicon.png
    - assets/imagefiles/trudiconRound.png
    # Screenshots & GitHub badge excluded - only for GitHub README, not bundled in APK

  # Variable fonts for customization and reduced APK size
  # Users can switch between fonts in Personalization settings
  fonts:
    # Inter - Modern, geometric typeface designed for UI (860 KB)
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf

    # Open Sans - Clean, neutral (520 KB)
    - family: OpenSans
      fonts:
        - asset: assets/fonts/OpenSans-Variable.ttf

    # JetBrains Mono - Monospace for code/technical notes (183 KB)
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Variable.ttf

    # Lexend - Enhanced readability (172 KB)
    - family: Lexend
      fonts:
        - asset: assets/fonts/Lexend-Variable.ttf

  # Roboto (system font) - Default Material Design, no asset needed

# Flutter launcher icons configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/icon/1.png"
  min_sdk_android: 21
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/icon/1.png"

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
