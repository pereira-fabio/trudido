# Pinned, like peakpace's, so a rebuild months from now produces the same image.
# No database driver: records are plain JSON files, so the store can live on a
# NAS share. SQLite is read only to import an older install, via the stdlib.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
# Blob uploads arrive as multipart form data.
python-multipart==0.0.9
